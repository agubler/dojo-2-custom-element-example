/*import { ProjectorMixin } from '@dojo/widget-core/mixins/Projector';
import { Menu } from './widgets/Menu';

const menuItemData = [
	{
		id: '1',
		title: 'Menu Item 1'
	},
	{
		id: '2',
		title: 'Menu Item 2'
	}
];

const Projector = ProjectorMixin(Menu);
const projector = new Projector();

function onItemSelected(selectedId: string): void {
	projector.setProperties({ menuItemData, onItemSelected, selectedId });
}

projector.setProperties({ menuItemData, onItemSelected });

projector.append();*/
